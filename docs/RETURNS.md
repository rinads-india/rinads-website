# Returns

Return lifecycle: `requested → approved → pickup_pending → received → inspected → approved_for_refund → rejected → completed`

Return lines support inspection and inventory disposition: `sellable`, `damaged`, `quarantine`, `discarded`.

Not all returns restock sellable inventory automatically.

Owner UI: `/returns`
