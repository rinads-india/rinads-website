# Suppliers

Entity: `Supplier` in `@rinads/operations` / table `suppliers`.

Fields: name, contact, phone, email, address, GSTIN, payment terms, status, notes.

Supplier products map variants to supplier SKU, cost, MOQ, lead time, preferred flag.

Owner UI: `/procurement/suppliers`
